import 'package:flutter/material.dart';

/// Soft pinkish background for product grid area
class AppColors {
  static const Color productBackground = Color(0xFFFFF8FC);

  /// Orange background for poster section
  static const Color posterBackground = Color(0xFFFF8748);

  /// The main theme orange (used in login buttons, highlights, etc.)
  static const Color primaryOrange = Color(0xFFFF8748);

  /// Optional theme blue (if needed later)
  static const Color primaryBlue = Color(0xFF2D9CDB);


  // White
  static const Color white = Colors.white;
  }
